# status.md

Single source of truth for project progress. Updated in the same commit as the work it
describes. See `CLAUDE.md` §14 for the protocol.

**Legend:** `todo` · `blocked` · `in-progress` · `review` · `done`

Acceptance cells stay short. A completed task's full write-up — design detail,
deviations, coverage, CI run — lives in `docs/notes/phase-N.md`, one frozen section
per task, and the row points there. Keep new completions to a summary in the cell
and put the story in the notes file, in the same commit.

No task is `blocked` as of 2026-09-01: every open question (Q1–Q25) is decided and all
Phase 0 ADRs have landed. A task's `Depends on` column still names the tasks that must
precede it — that is ordering, not a blocker.

---

## Phase 0 — Decisions (Fable 5, no code)

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| D-001 | Confirm project name + module path | done | Q1 | Name agreed; `go.mod` path recorded in CLAUDE.md |
| D-002 | ADR: scanner choice (library vs binary) | done | Q2 ✓ | `docs/adr/0017-scanner-integration.md` — Trivy-as-library behind adapter, golden corpus, table-backed queue, air-gapped DB import |
| D-003 | ADR: authentication model | done | Q3 ✓ | `docs/adr/0004-authentication-model.md` — users, robots, PATs, OCI token flow, UI sessions, bootstrap/recovery |
| D-004 | ADR: HA posture for v1 | done | Q5 ✓ | `docs/adr/0018-ha-posture.md` — single node + lock file, seam table, no memory-resident correctness state |
| D-005 | ADR: UI stack + design direction | done | — | `docs/adr/0019-ui-stack.md` — Svelte 5 + Vite static SPA, near-zero runtime deps, offline pnpm builds; recorded in CLAUDE.md §10 |
| D-006 | ADR: metadata schema + migration strategy | done | D-017 ✓, D-011 ✓ | `docs/adr/0006-metadata-schema.md` — full ERD, hosted/cached table families, forward-only per-engine migrations |
| D-007 | ADR: blob storage layout + digest verification | done | — | `docs/adr/0007-blob-storage-layout.md` — layouts, atomic commit, stream-verify + quarantine, disjoint store instances |
| D-008 | ADR: retention/GC safety model | done | D-007 ✓ | `docs/adr/0010-retention-gc-safety.md` — pure evaluator + plans, mark-and-sweep with grace window, proof sketch, race matrix |
| D-009 | Choose license | done | Q9 ✓ | Apache-2.0; canonical `LICENSE` committed (repo went public ahead of F-001) |
| D-011 | ADR: repository model — hosted / proxy / group | done | D-017 ✓ | `docs/adr/0005-repository-model.md` — prefix routing, pure group resolution, write rules, lifecycle (dep flipped: D-006 now consumes this) |
| D-012 | ADR: cache semantics | done | Q11 ✓ | `docs/adr/0008-cache-semantics.md` — leases, negative cache, offline modes, single-flight, backoff, eviction defaults |
| D-013 | ADR: cache eviction vs retention separation | done | D-007 ✓, D-011 ✓ | `docs/adr/0009-eviction-retention-separation.md` — four walls: types, wiring, imports, schema; proving test defined |
| D-014 | ADR: event model + webhook delivery | done | D-006 ✓ | `docs/adr/0012-events-webhooks.md` — taxonomy, outbox pattern, live authz filtering, SSRF validation, retry/DLQ |
| D-015 | ADR: pull gating design | done | Q12 ✓, Q24 ✓ | `docs/adr/0013-pull-gating.md` — single enforcement door, bypass closure, fail-closed lookups, audited break-glass |
| D-016 | ADR: quota accounting model | done | Q8 ✓ | `docs/adr/0014-quota-model.md` — attribution vs physical accounting, hysteresis soft-warn, 413+DENIED, cache evicts |
| D-017 | **ADR: RBAC model** | done | Q14 ✓, Q19 ✓, Q20 ✓ | `docs/adr/0001-rbac-model.md` — subjects, groups, roles, bindings, scope grammar, built-in roles |
| D-018 | **ADR: permission vocabulary** | done | D-017 ✓ | `docs/adr/0002-permission-vocabulary.md` — 30 verbs incl. `repo:create`/`repo:configure`, splits justified, verb → operation mapping (count corrected from 33 in Z-005: the ADR's tables always held 30) |
| D-019 | **ADR: visibility & disclosure policy** | done | Q18 ✓ | `docs/adr/0003-visibility-disclosure.md` — status-code matrix, ten enumerated filtered surfaces |
| D-020 | ADR: admin API + CLI conventions | done | Q23 ✓ | `docs/adr/0015-admin-api-cli.md` — /api/v1, problem+json, cursor pagination, spec-vs-route CI check, offline exceptions |
| D-021 | ADR: secrets key management | done | Q21 ✓ | `docs/adr/0016-secrets-key-management.md` — keyfile + key-id format, AAD context binding, two-step rotation, redaction rules |
| D-022 | ADR: referrer lifecycle on subject deletion | done | Q22 ✓ | `docs/adr/0011-referrer-lifecycle.md` — transitive cascade, plan visibility, live-attachment protection, orphan sweep |
| D-010 | Decompose Phases 1–8 into implementable tasks | done | D-001..D-022 | `docs/plan/phase-{1..8}-*.md` — every task has criteria, files, deps, test plan, and per-phase parallelization notes |

---

## Phase 1 — Foundations

Detailed specs (files, test plans): `docs/plan/phase-1-foundations.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| F-001 | Repo scaffold, module, Makefile, CI skeleton | done | D-001 ✓, Q25 ✓ | `make build test lint cover test-linux` green locally; CI green first run (run 33547896799): vet, gofmt, golangci-lint v1.64.8, race suite, 3-target cross-compile. Coverage 100% excluding `cmd/trove/main.go` |
| F-002 | Coverage gate script (≥95%, `-coverpkg=./...`) | done | F-001 ✓ | `scripts/coverage.sh` + 10-case self-test + shellcheck, all green in CI (run 33548479273). Drop proven: uncovered func → 89.1%, exit 1. Merges duplicate blocks; degenerate inputs exit 2 |
| F-003 | Config load/validate/defaults | done | F-001 ✓ | `internal/config`: precedence flags > env > file > defaults with per-key source tracking; every validation problem reported at once; secrets redacted with no unredacted renderer. Details → `docs/notes/phase-1.md` |
| F-004 | Structured logging + graceful shutdown | done | F-001 ✓, F-003 ✓ | `internal/server`: slog by config, request IDs echoed and context-carried, levelled access log, graceful drain proven in-flight, data-dir lock. Details → `docs/notes/phase-1.md` |
| F-005 | `meta.Store` core: repositories + content | done | D-006 ✓ | `internal/meta` types/errors/Visibility, `metatest` 24-case contract suite incl. exhaustive per-method cancellation, `memory` reference impl green under `-race`. Coverage 98.6%. Amends §9: shared test harnesses excluded from the gate; CI green (run 33555464491) |
| F-005b | `meta.Store` identity + authz groups | done | F-005 ✓ | Subjects (incl. undeletable anonymous), local groups, roles with read-only built-ins, bindings, and `ListEffectiveBindings` with group provenance; 14 added contract cases, 164 subtests total. Coverage 98.7%. Credentials/sessions deferred to F-005c; CI green (run 33557505946) |
| F-005c | `meta.Store` credentials + sessions | done | F-005b ✓ | Argon2id verifiers, robot secrets with mandatory expiry, PATs, sessions with idle+absolute bounds; hashes only, expiry enforced on read, subject deletion cascades. 11 added cases, 219 subtests total. Coverage 98.9%; CI green (run 33559361399) |
| F-006 | SQLite impl + migrations | done | F-005 ✓ | `internal/meta/sqlite` (modernc, no CGO): WAL, verified foreign keys, single-writer pool, embedded forward-only migrations; `--no-auto-migrate` refuses naming what is pending. Details → `docs/notes/phase-1.md` |
| F-007 | Postgres impl | done | F-005 ✓, F-006 ✓ | `internal/meta/postgres` (pgx/v5): contract suite green unmodified against testcontainers `postgres:17`; schema-parity test holds both engines to identical columns. Details → `docs/notes/phase-1.md` |
| F-008 | `blob.Store` interface + contract test suite | done | D-007 ✓ | `internal/blob`: Store/Uploader/UploadSession per ADR 0007; the strict digest parser is the traversal gate (fuzzed: nothing accepted can carry a path); shared Verifier/Copy/VerifiedReader. Details → `docs/notes/phase-1.md` |
| F-009 | Filesystem blob driver | done | F-008 ✓ | `internal/blob/fs`: ADR 0007 layout, staging → fsync → rename atomicity (a blob path is absent or complete), corrupt reads quarantined, never served. Details → `docs/notes/phase-1.md` |
| F-010 | S3-compatible blob driver | done | F-008 ✓ | `internal/blob/s3` (minio-go): same key scheme under a prefix, multipart commit only after the digest verifies, contract suite green against a MinIO container. Details → `docs/notes/phase-1.md` |

---

## Phase 2 — Identity and RBAC

Built before the registry handlers, not after. Retrofitting permission filtering into
existing queries is how disclosure bugs get shipped.

Phase 1 closed 2026-09-02 (F-001…F-010): config, logging and shutdown, `meta.Store`
with SQLite and Postgres, and `blob.Store` with filesystem and S3 drivers, each
interface with a contract suite every implementation runs unmodified.

Detailed specs (files, test plans): `docs/plan/phase-2-identity-rbac.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| Z-001 | Subject model: users, robot accounts, anonymous | done | D-003 ✓, F-006 ✓ | `internal/authn`: one `Resolve` for users, robots, and the seeded anonymous row — no nil-subject branch anywhere; disabling the row turns anonymous off wholesale. Details → `docs/notes/phase-2.md` |
| Z-002 | **Users + Argon2id credentials** | done | D-003 ✓, Z-001 ✓ | Argon2id in PHC form with hash-carried params bounded against decompression DoS (fuzz found it twice); GCRA limiter per account and per source with an honest Retry-After. Details → `docs/notes/phase-2.md` |
| Z-003a | **Secrets encryption (`internal/secretbox`)** | done | D-021 ✓ | `internal/secretbox`: AES-256-GCM sealed values (`v1:<key-id>:`, mandatory AAD, multi-key rotation), pinned golden; sole AEAD importer, enforced by archtest. Details → `docs/notes/phase-2.md` |
| Z-003b | Robot accounts: mandatory expiry, revocation | done | D-003 ✓, Z-001 ✓, Z-003a ✓ | Robot accounts: HMAC-digested secrets at rest, one active secret, mandatory expiry enforced on read, revocation wins at next use; `robot$` dispatch in basic auth. Details → `docs/notes/phase-2.md` |
| Z-004 | OCI token flow (`WWW-Authenticate` / bearer) | done | D-003 ✓, Z-002 ✓, Z-003b ✓, Z-008 ✓ | OCI token flow: Ed25519 JWTs (library quarantined), grants intersected via `authz.Allows`, `/v2/` probe + challenge; revoking a binding kills a live bearer next request; docker login sequence proven over HTTP. Details → `docs/notes/phase-2.md` |
| Z-005 | **Permission vocabulary as typed constants** | done | D-018 ✓ | The 30-verb vocabulary as a closed set + `verbtest`: the §9 both-polarities ratchet that only shrinks. Details → `docs/notes/phase-2.md` |
| Z-006 | **Role model + built-in roles** | done | D-017 ✓ | The six ADR 0001 built-in roles; `admin`/`operator`/`auditor` derived from the vocabulary so they cannot fall behind it; DB seeding deferred to Z-014. Details → `docs/notes/phase-2.md` |
| Z-007 | **Binding model + repository pattern matching** | done | D-017 ✓, Q20 ✓ | `internal/authz/{scope,scope_sql,binding}.go`: the four-form grammar (`system` \| `*` \| exact \| trailing `/*`) as one validator, one matcher, and one filter compiler for the query layer; `Resource` is a struct so the `system` keyword and a repository named `system` cannot be confused, and its zero value matches nothing. Scope patterns validate against the repository-name grammar, now in the leaf package `internal/reponame` (new — authz cannot import `repo` under Z-009, and the registry needs the same rule in Phase 3). Decision made here: the whole `system` prefix is reserved, so `system/*` is refused rather than silently meaning a repository directory — a binding written that way by somebody meaning the global scope would grant nothing while looking like it granted everything; C-016 must refuse the matching repository names. Three fuzz targets green (`FuzzParseScope`, `FuzzScopeMatches`, `FuzzScopeMatcherAgreesWithSQL` — 1.6M+ execs, well past the 10k the plan asks for), plus `FuzzValidate` on the name grammar; the differential test runs the query layer's own predicate through SQLite and also checks `meta.ScopeFilter`, so all three readings of a pattern agree. Coverage 100% of both packages, 96.2% overall; CI green (run 33729537147) |
| Z-008 | **`authz.Decide` pure decision function** | done | Z-005 ✓, Z-006 ✓, Z-007 ✓ | `authz.Decide`: pure, returns every matched binding (the explainer is this call, not a second implementation); five fuzzed properties. Details → `docs/notes/phase-2.md` |
| Z-009 | **Import-boundary test for `internal/authz`** | done | Z-008 ✓ | `internal/archtest`: allowlist rules over `go list -deps` — authz reaches no storage, the cache↔gc wall holds both ways, primitives stay quarantined; verified by injecting a real violation. Details → `docs/notes/phase-2.md` |
| Z-010 | **Handler-level enforcement middleware** | done | Z-008 ✓, Z-001 ✓ | The Guard: subject → bindings → `Decide` — one path for every request, registration demands a Permission, ADR 0003's matrix is a table test including the byte-identical 404. Details → `docs/notes/phase-2.md` |
| Z-011 | **Route-table guard test (fail closed)** | done | Z-010 ✓ | `Router.Verify` + frozen public-route list + mux quarantine (AST scan): a table Verify rejects refuses to dispatch, and no route can exist outside it. Details → `docs/notes/phase-2.md` |
| Z-012 | **Permission-filtered query layer** | done | D-019 ✓, Z-007 ✓, F-006 ✓, F-007 ✓ | `VisibilityFor`: the one bindings→filters bridge, never Unrestricted for a subject; filtered-pagination and cursor-stability contract tests; `repo:list` off the §9 list. Details → `docs/notes/phase-2.md` |
| Z-013 | **Effective-permission explainer** (CLI + API) | done | Z-008 ✓, D-020 ✓ | `/api/v1/auth/explain` renders `Decide` itself so it cannot drift; introduces `Permission.Self`; refusal identical whether the asked-about subject exists; CLI is a client of the endpoint. Details → `docs/notes/phase-2.md` |
| Z-014 | Admin bootstrap + forced first-login rotation | done | Z-002 ✓, Z-006 ✓ | Bootstrap: built-ins converge every boot, admin created only when nobody could log in, password printed once to stdout with forced rotation; shared `PasswordLogin` (limits, decoy verifier); must-rotate gate; `trove serve` becomes real. Details → `docs/notes/phase-2.md` |
| Z-015 | Self-lockout prevention | done | Z-007 ✓ | Last-admin lockout: `EnsureAdminRemains` refused across every §5 removal vector including role-verb narrowing; enforcement contract documented for C-016/U-007's mutation handlers. Details → `docs/notes/phase-2.md` |
| Z-016 | `authz.denied` + `role.changed` events and metrics | todo | Z-010 ✓, E-001 | Denials counted by verb; role changes audited before/after |
| Z-018 | **Disclosure adversarial suite** | done | D-019 ✓, Z-012 ✓ | The disclosure suite over ADR 0003's surfaces, with a status.md-reading skip ratchet: a surface cannot go `done` without its test landing in the same commit. Details → `docs/notes/phase-2.md` |
| Z-019 | **Privilege-escalation adversarial suite** | done | Z-010 ✓, Z-004 ✓ | The privilege-escalation suite: every ADR 0002 non-implication pinned at `Decide` with an ADR-parsing drift check; robot repo-crossing and post-revocation token replay end-to-end. Details → `docs/notes/phase-2.md` |
| Z-020 | UI session auth: browser login, sessions, CSRF | todo | D-003 ✓, D-020 ✓ | Separate from the OCI token flow; CSRF + session-fixation tested; rate-limited |

---

## Phase 3 — OCI registry core (hosted)

Detailed specs (files, test plans): `docs/plan/phase-3-registry-core.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| R-001 | Blob upload (monolithic, chunked, resumable) | done | F-009 ✓, Z-010 ✓ | Distribution blob API (start/chunk/commit/status/cancel + cross-repo mount) over new `HandleOCI` routing; /v2/ speaks the spec envelope; every failed mount is an identical 202 (no probing); a mismatched commit publishes nothing. Details → `docs/notes/phase-3.md` |
| R-002 | Manifest put/get/delete + media-type validation | done | R-001 ✓, F-006 ✓ | Manifest PUT/GET/DELETE + `internal/artifact` parser (closed set of four media types); missing layer/child → `MANIFEST_BLOB_UNKNOWN`; ADR 0011 cascade with Q10 checked tree-wide and failing closed; reads re-hash (drift is a 500); `manifest:delete` enforced both ways. Details → `docs/notes/phase-3.md` |
| R-003 | Tag list + pagination (permission-filtered) | done | R-002 ✓, Z-012 ✓ | `tags/list` under `repo:read`, filtered end to end, keyset `n`/`last` pagination with spec `Link`; hidden ≡ absent byte-for-byte; disclosure surface 2 unskipped. Tag deletion landed later via the route split the notes predicted: `{reference:digest}` and `{reference:tag}` are two routes under `manifest:delete` and `tag:delete`, told apart before the guard decides — **`tag:delete` off the §9 list (17 left)**. Details → `docs/notes/phase-3.md` |
| R-004 | Catalog endpoint (permission-filtered) | done | Z-012 ✓ | `/v2/_catalog` as the first Listing route: guard-computed Visibility, so counts and cursors come pre-filtered; anonymous with nothing visible is challenged; disclosure surface 1 unskipped. Details → `docs/notes/phase-3.md` |
| R-005 | Referrers API (v1.1) | done | R-002 ✓ | Referrers API: `referrer:read` ∧ `repo:read` (checked before the digest parses), assembled OCI index with `artifactType` filter + `OCI-Filters-Applied`, golden body, digest ordering now interface contract; surface 3 unskipped; `referrer:read` off the §9 list. Details → `docs/notes/phase-3.md` |
| R-006 | Multi-arch index handling | done | R-002 ✓ | Gap-closing over R-002: the delete-order matrix (`internal/registry/index_test.go`) — shared children, nested indexes, Docker lists, platform byte-round-trip — plus a mutation-caught test that alone pins the handler's tree-wide Q10 pre-check, and child descriptors now size-checked like blob descriptors. GC fixture deferred to P-007; oras fixtures to R-009. CI green (run 33882812060). Details → `docs/notes/phase-3.md` |
| R-007 | Helm chart + SBOM artifact awareness | done | R-005 ✓ | `internal/artifact/kinds.go` — `Kind` labels images, indexes, Helm charts, SBOMs (SPDX/CycloneDX), signatures (cosign/sigstore) and attestations (in-toto/SLSA), with `KindOfStored` answering from the two columns ADR 0006 already keeps, so no migration and nothing to drift. **Labeling only**: an unrecognised artifact type stores, serves and refers exactly like a known one and simply reads `unknown`. `Kind.IsSignature` is ADR 0013's presence-check input, so gating asks about meaning rather than matching media-type strings of its own. Round-tripped through the real handlers with the payload shapes `helm push` and `oras attach` send; binary-driven integration belongs to R-009's harness. CI green (run 33902922424). Details → `docs/notes/phase-3.md` |
| R-008 | Spec error-code mapping | done | R-002 ✓, D-019 ✓ | 24 goldens pin the /v2/ wire contract byte-for-byte (`internal/registry/testdata/errors/`): every `SpecErrors` renderer branch (incl. the empty-challenge and Retry-After-floor branches), one envelope per `Code*` constant, and the JSON HTML-escaping nobody chose but clients now depend on. An AST-driven ratchet means a new code cannot ship unpinned; zero rendered bytes changed. CI green (run 33882812060). Details → `docs/notes/phase-3.md` |
| R-009 | OCI conformance suite in CI | done | R-001..R-008 ✓ | **73 of 73 applicable specs pass** — pull, push, content discovery, and content management, run by a CI job against a real spawned `trove serve` (`test/conformance/`, upstream suite pinned at v1.1.1). The harness owns trove's bootstrap (one-time password, forced rotation, repository creation via C-016) and has its own sanity test that is the project's first real push/pull over a socket. **The run found two production bugs**: the auth limiter charged *successful* logins, so any client minting more than a burst of tokens was cut off mid-push; and an unrouted blob DELETE 404'd where the spec wants 405, hiding "we do not do that" behind "it is not here". Group pull-side conformance is a tracked skip until C-012. CI green (run 33905885088). Details → `docs/notes/phase-3.md` |
| R-010 | Pull statistics: last-pulled + counts | done | R-002 ✓ | `meta.RecordPulls`/`GetPullStats` (all three engines, migration 0003, contract cases incl. the failure tables) + `registry.PullBatcher` on `Manifests.Pulls`. Proven: 50 GETs through the real stack → **zero** store calls, one batched upsert on flush. Counts accumulate, last-pulled never regresses, no FK so a repointed tag cannot fail a flush; HEAD deliberately not counted. `artifact.pulled` event waits on E-001. CI green (run on 87db1ba). Details → `docs/notes/phase-3.md` |
| R-011 | Stale upload-session reaping | done | R-001 ✓ | `internal/registry/upload_reaper.go` + `registry.upload_session_ttl` (default 24h) + serve's interim hourly loop (P-006 replaces the loop, keeps `ReapOnce`). Staging cancelled before the row so every crash window self-heals; activity refreshes the clock so active uploads are structurally unreachable; committed blobs untouched by construction and by test; per-session failures logged, joined, and never stop the sweep. CI green (run 33882812060). Details → `docs/notes/phase-3.md` |
| R-012 | Push-latency benchmark | done | R-001 ✓, R-002 ✓ | Four benchmarks through the real router (`internal/registry/bench_test.go`) + `scripts/bench-check.sh` (39-assertion self-test) + a parallel CI `bench` job. Best-of-N against a committed baseline with 20% tolerance; the baseline ships absent by design — the first CI run records it as an artifact, the operator commits it, then the gate enforces. Scan-backlog half waits on S-003. CI green (run 33884250982); baseline from that run committed, gate armed. Details → `docs/notes/phase-3.md` **Amended 2026-09-07 (C-015):** the committed-baseline gate was machine-specific and `ubuntu-latest` is not one machine — it now compares against the **parent commit measured on the same runner**, and the committed baseline is advisory drift context. Details → `docs/notes/phase-4.md`. |

---

## Phase 4 — Proxy, cache, and groups

Detailed specs (files, test plans): `docs/plan/phase-4-proxy-cache-groups.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| C-001 | Repository model + router (hosted/proxy/group) | done | D-011 ✓ | `internal/repo`: `Split` (first-segment router, fuzzed to 6M+ execs: never accepts what the grammar rejects, always reassembles), `ValidateEntityName` (one segment, `system` reserved per Z-007 — the check C-016 creates through), `Writable` (a function of type alone — no config parameter exists through which a proxy could become writable), and typed configs with strict parsing (proxy: upstream URL hygiene incl. credential refusal, routing patterns in the one binding-scope grammar, TTL/offline overrides; hosted/group deliberately empty). The serving path is integrated: handlers route by entity prefix (content keyed by full name; migration 0004 moves the entity rule from a full-name FK into the stores' prefix check), the catalog lists content names via the new contract-tested `meta.ListContentNames`, and `DeleteRepository`'s doc-vs-behavior drift was fixed in favor of the cascade the suite pinned — implemented over a range, not `LIKE`, because `_` in a name would wildcard into a neighboring entity. CI green (run on 1672898). Details → `docs/notes/phase-4.md` |
| C-002 | Upstream client interface + contract suite | done | D-012 ✓ | `internal/proxy`: the frozen `Client` (conditional `ResolveTag`, digest-verified `FetchManifest`/`FetchBlob`, `RateLimit()`), with a **total** sentinel mapping so no caller ever branches on a status code — including `ErrRedirectRefused` kept distinct from unavailability, because an SSRF attempt must not hide behind a stale-content warning. Digest work reuses `internal/blob` throughout: mismatch **is** `blob.ErrDigestMismatch`, one verification and one error. **SSRF policy is pure and table-tested** (40 rows): 5-hop cap, no https→http judged against upstream *and* hop so a chain cannot launder a downgrade, private/loopback refused before `TrustedHosts` is consulted, and the same policy guards the `WWW-Authenticate` realm — which is where the operator's password would go. Contract suite runs against **both** `registry:2` and the fake, which is what makes the fake provably contract-equivalent for C-004+. `clienttest` added to the §9 harness list (deliberate edit, with a self-test counter-assertion). CI green (d928a0a). Details → `docs/notes/phase-4.md` |
| C-003 | Upstream credential storage (encrypted, redacted) | done | D-003 ✓, D-021 ✓ | `internal/proxy/credentials.go` + migration 0007. **The absence is structural, not a rule handlers follow**: `server.RepositoryAdminStore` omits `GetProxyCredential` entirely, so the handler's type has no method that returns a sealed value; the status is a *distinct type* rather than the same one blanked (a blanked field is one assignment from being populated) and its SQL never selects the ciphertext column. Sealed `{username,password}` under `secretbox.ProxyCredential(<repo>)` — username sealed too, since half a credential in the clear is the half that names who the token belongs to. Every read path probed by `strings.Contains` over the **whole body** (never a named field) for password, username, ciphertext and a bare `v1:` prefix, across five subjects incl. every-verb admin, the listing, config echo, and three refusal bodies; probes proved non-vacuous by opening the row afterwards. **AAD swap test** proves a row moved between repositories fails authentication while the original still opens. **`proxy:credentials` off the §9 list (16 left)**. Details → `docs/notes/phase-4.md` |
| C-004 | Blob/manifest fetch-and-cache by digest | done | C-002 ✓, F-009 ✓ | The cached-content family ADR 0006 named and nothing had built: `meta.CachedManifest`/`CachedManifestRef`/`CachedBlob` behind the new `CachedContentStore`, over **migration 0008** in both engines, swept by `DeleteRepository` beside the hosted tables (the one operation that legitimately spans both families) with `last_access_at` indexed from the start because a migration over the largest tables later is not free. **Every cached write requires a proxy entity**, not merely an existing one — cached content is defined by being refillable, so a row under a hosted entity would claim recoverability the deployment cannot deliver; a contract case writes one digest into both families and proves each answers only through its own methods. `internal/proxy/fill.go` streams a blob to the client and into an upload session at once and publishes nothing until the whole stream verified, so a disconnect, a truncation and a lying upstream all leave the cache untouched; bytes land before the row deliberately (a row without bytes is a miss the read path handles; bytes without a row wait for C-013's orphan pass). **A cache failure is not a pull failure** — every write failure is served through with a flag saying the cache does not hold it, while a cache *read* failure is returned rather than becoming a stampede against somebody else's registry. An unparseable manifest is refused as `ErrUpstreamUnavailable`, so a group member answering with one is treated exactly like one that is down. Not yet wired into serve: C-005 brings the lease and the wiring. CI green (run 34053814871). Details → `docs/notes/phase-4.md` |
| C-005 | Tag → digest lease with TTL + revalidation | done | Q11 ✓, C-004 ✓ | `internal/proxy/lease.go` + **migration 0009** (`tag_leases`, both engines, swept with the cached family). Three outcomes: a lease inside its TTL answers with no upstream request; an expired one revalidates **conditionally** (unchanged transfers no body — C-002's suite proves that against registry:2 and the fake) and moves only the deadline; a changed tag caches the new manifest *from the bytes the resolution already carried* and repoints the lease while **the old manifest stays cached**, so an image pinned by digest keeps working. Column semantics decided here: `fetched_at` is when the mapping was last *confirmed*, `ttl_s` is recorded but expiry uses the repository's *current* TTL (lowering it takes effect on the next pull), and `stale` means "the last revalidation could not be completed and this was served anyway" rather than the derivable "past its TTL". **Degraded mode covers outages only** — unreachable and 429 serve stale with `cache.stale-served`, `strict` fails, while a rejected credential or a refused redirect is returned as itself because serving through either would replace a loud failure with a quiet one; the stale path marks the lease but never moves `fetched_at`, so one unreachable upstream cannot buy a full TTL of silence. `TagTTL`/`Offline` are per-call Target fields since zero TTL means revalidate-every-pull (Q11) and cannot double as "unset". Single-flight (C-006), negative caching (C-007) and the `/v2/` wiring are deliberately still absent. CI green (run 34054517067). Details → `docs/notes/phase-4.md` |
| C-006 | Single-flight on concurrent cache fill | done | C-004 ✓ | `internal/proxy/coalesce.go`: `Coalescer` is ADR 0018's seam, `SingleFlight` its v1 body (`x/sync/singleflight`), wired into tag resolution and manifest fetch. **The shared work runs detached from cancellation while each caller waits under its own context** — the leader is not special, so its client disconnecting must not fail the fifty waiters behind it, and a waiter leaving must not be held by work it no longer needs; the client's request timeout is what bounds the detached fetch. The manifest payload is **copied out of the shared result**, because before coalescing every caller held its own bytes and being handed somebody else's buffer only when you happened to race is a data race waiting to happen. **Blobs are deliberately not coalesced**: a stream has one reader, and two concurrent layer fills already converge at the content-addressed store (C-004). Concurrency is tested without flake by blocking the leader inside the upstream client before any waiter starts; the exactly-once counts sit in the success cases (a straggler reads the cache, so the upstream is still called once), while the failure case asserts propagation instead — a failed flight caches nothing, so a late caller correctly retries — backed by a deterministic recording-`Coalescer` test that pins both keys and proves the blob path is not one of them. CI green (run 34054971314). Details → `docs/notes/phase-4.md` |
| C-007 | Negative cache for upstream 404s | done | C-005 ✓ | **Migration 0010** `negative_cache` + `internal/proxy/negative.go`. **Names only** — `Manifest` and `Blob` never read or write the table, proven by a case that misses a digest three times, finds no row, plants one by hand and shows the digest path ignores it (a digest absent now may exist a moment later, and caching that would break push-then-pull-through-a-group). **A zero `NegativeTTL` switches it off**, the opposite of `TagTTL`'s zero and deliberately: "confirm every time" is the safe reading of a mapping, "believe it forever" is not the safe reading of an absence; freshness uses the repository's current TTL so the switch takes effect next pull. The entry is **cleared when the upstream answers** rather than left to expire, which keeps the table tracking live mistakes and gives the delete a caller, and a remembered absence returns the upstream's own `ErrNotFound` so no caller can tell it from a fresh one. Store failures follow the subsystem's pattern: an unwritable or uncleanable record costs one request and is logged; an unreadable one fails the resolution rather than becoming a stampede. CI green (run 34055413620). Details → `docs/notes/phase-4.md` |
| C-008 | Offline / degraded mode | done | C-005 ✓ | `internal/proxy/degraded.go`: `Classify` turns a failure into one of four causes (`unreachable`, `timeout`, `upstream-error`, `rate-limited`) or says it is not degraded at all. **It reads the client's typed errors, never the message text** — a transport failure carries the net error it wrapped and a status failure the status, which is what lets a resolver that timed out be told from one that answered "no such host" after both have been through net/http. The rate-limit check matches the *sentinel* rather than the struct so a foreign `Client` is classified by the contract it is held to; the client's own header deadline is recognised through its internal sentinel; anything else still claiming unavailability counts, since silently not covering an unrecognised implementation would disable stale-serving for it. Rejected credentials, refused redirects, invalid references and oversized manifests stay uncovered — an SSRF attempt must never end up behind a stale-content warning. **The fault matrix drives real client errors through a real transport** (NXDOMAIN and timeout dialers, refused, reset, dial deadline, stalled headers against the 50ms timeout, 500, 429, rejected token, off-host redirect, genuine 404), and the blackholed-DNS case proves the acceptance criterion end to end: cached served stale with the event naming `unreachable`, uncached failing cleanly, `strict` flipping the answer on the next call with no restart. The `Warning: 110` header waits for the HTTP path (`Stale`/`StaleFor` are what it will render from) and the backoff waits for C-009. CI green (run 34055751891). Details → `docs/notes/phase-4.md` |
| C-009 | Rate-limit handling + upstream quota metrics | done | C-002 ✓ | `internal/proxy/backoff.go`. **Keyed by proxy entity**, because that is the unit a quota and a credential belong to — keying by content name would let a hundred repository paths discover the same throttling separately, which is the storm this prevents. The delay is the longest of the upstream's `Retry-After` and a jittered exponential, then **capped (5m default) including the upstream's own number**: a proxy quiet for the six hours Docker Hub sometimes asks for is indistinguishable from a broken one, and one probe per cap costs nothing. **While backing off the proxy behaves exactly as when the upstream is unreachable** — the synthesized error is the same `*RateLimitedError` a real 429 produces, so C-008 classifies it and C-005's degraded path serves stale, fails uncached, and fails everything under `strict`; digest fetches are bounded by the same window. Acceptance proven: fifty pulls in one window cost the upstream one request. **Only throttling backs off** (an outage is retried next pull; a success clears the standing completely), and adversarial cases pin that forty refusals land on the cap rather than wrapping into an instant retry and that neither hostile jitter nor a negative `Retry-After` can pull the deadline into the past. **The Prometheus half is deferred to E-005/E-006 on purpose** — they own the registry, the exposure modes and the `metrics.per_repo` label rule a per-proxy gauge would pre-empt; `Backoff.Snapshot` plus C-002's `Client.RateLimit()` are the data they will read. CI green (run 34057879425). Details → `docs/notes/phase-4.md` |
| C-010 | Routing rules (allow/block patterns per proxy) | done | C-001 ✓ | `internal/repo/routing_rules.go`: patterns compile once into `authz.Scope` values — the binding grammar, not a second matcher — and evaluate block > allow > default (allow, or deny when `default_deny`). The decision names *which* rule matched, because an operator debugging a blocked pull needs that. An illegal remainder is an error under **both** defaults rather than falling through to allow, so traversal cannot reach the upstream URL builder. **Reconcile decision:** rules evaluate the *rewritten upstream path*, since they exist to stop an open relay and only that reading lets `library/*` admit `nginx` on the Docker Hub preset. Fuzzed 8.1M execs. CI green (d928a0a). Details → `docs/notes/phase-4.md` |
| C-011 | Group resolution (ordered, first-match-wins) | done | C-001 ✓ | `internal/repo/group.go`: `Resolve(members, reference)` — no store, no clock, no I/O, so C-012 filters before calling and the whole matrix is table-testable. First member by position that serves wins; not-found is passed over; down or malformed is skipped with a `group.member.skipped` payload unless `Required`, which fails the group 503-class; an empty member list answers exactly like all-not-found, which is what makes C-012's filtering invisible. Nesting, unknown types, and duplicate positions or names are refused whole before the walk. Fuzzed 4.4M execs for determinism under permutation. CI green (d928a0a). Details → `docs/notes/phase-4.md` |
| C-012 | **Permission filtering before group resolution** | done | C-011 ✓, Z-012 ✓ | `internal/repo/group_filter.go`. **The wall is the type**: `Resolve` now takes a `MemberSet`, buildable only by `VisibleMembers` (filters for a subject) or `AllMembers` (says in as many words that the caller has none) — the shape `meta.Visibility` already uses, adopted here as a deliberate change to C-011's signature because the alternative is a comment asking callers to remember. **The decision is `repo:read` against the member's *content name*, not the entity**: a subject scoped `dockerhub/*` may read `dockerhub/library/nginx` and not the bare name, so deciding against the entity would hide a member whose content they are entitled to; `MemberState.Content` carries it and a member without a legal one is dropped fail-closed. **Removed, not skipped** — a skipped member appears in the resolution and in the event that names it, a removed one nowhere; the sharpest case is a *required* down member, which fails the group for a subject that may read it and does not exist for one that may not. Positions keep their gaps (renumbering would make the ordering depend on what a subject cannot see) and the set copies both ways. **The disclosure suite's group surface is unskipped** and runs the real pipeline with differential assertions (carol's answer compared field for field against a group configured without the hidden member; anonymous's against an empty group). The HTTP half waits for the task that mounts group serving on `/v2/` — **which no status.md task currently owns**. CI green (run 34058346159). Details → `docs/notes/phase-4.md` |
| C-013 | Cache eviction (size/LRU budget) | done | D-013 ✓, Q11 ✓, C-004 ✓ | **The evictor landed** (commit 2 of 2), over part 1's store layer. `Evictor.Sweep` enforces per-proxy carve-outs *before* the global budget — a global-first pass would let one greedy upstream's fills evict a well-behaved proxy's content while the greedy one stayed over its own ceiling — and goes under the budget by a headroom fraction, because a sweep that stops at the line is triggered again by the next byte cached. Manifests and blobs rank in one order since they compete for one budget. **Bytes are reclaimed only when the last claim goes**: `DeleteCachedBlob` returns the remaining claims from inside its own transaction, and what it does not reclaim is counted as `Shared` rather than hidden. A failed byte delete is not a sweep failure — the row is gone, the accounting is right, and the bytes become an orphan. `SweepOrphans` collects those (bytes a fill committed before its row, by C-004's deliberate ordering), keeping everything when the claims query fails: "I do not know" reads as "do not delete". The `TouchBatcher` writes LRU times off the pull path in R-010's shape, plus a `Flush` the pull batcher has no need for — the Scheduler flushes before every sweep, or eviction would rank content served seconds ago as the coldest thing in the cache. The Scheduler coalesces breach triggers through a depth-one channel (a hundred fills during one sweep leave one sweep pending), pins the orphan pass to every Nth sweep rather than a drifting second timer, keeps running after a failed sweep, and says nothing at all about a cancelled one. **ADR 0009 wall 1's blob newtypes landed with their first deleting caller** as the ADR said they would: `blob.CachedRef`/`HostedRef` wrap a validated digest in a struct, so crossing is not a conversion; `internal/cache`'s `reclaim` is the only function that deletes cache bytes and takes the former. **The proving test is `test/separation`** — in the top-level tree because wall 3 means no constrained package can host it — over one fixture holding both families *with the same digests*: disjoint roots and cached-only wiring (a), the two import rules present and passing (b), a retention plan reaching only hosted rows (c), and an eviction pass calling only cached-family methods (d). `internal/cache` is at 100% line coverage; module lint clean. **Not wired into serve**: the wiring task constructs these and decides where a per-proxy carve-out is configured from (`repo.ProxyConfig` has no budget field yet, deliberately). Coverage 96.4% (`internal/cache` 100%); CI green (run 34126240796), conformance included. Details → `docs/notes/phase-4.md` |
| C-014 | Default upstream presets | done | Q7 ✓, C-001 ✓ | `internal/repo/presets.go`: five proxy configurations (`dockerhub`, `gcr`, `ghcr`, `k8s`, `quay`) shipped as **data** — nothing instantiates them, so Q7's "disabled" is a property of the deployment rather than a flag. Each carries the `trusted_hosts` list its registry actually needs (Docker Hub mints tokens at `auth.docker.io` and serves layers from a CDN under `docker.com`; quay's list is empty *on purpose*, its CDN being a subdomain of the upstream), which is why **`repo.ProxyConfig` gained `TrustedHosts`** — the field C-002 flagged as missing. That made two packages need one grammar, so it moved to the new leaf package **`internal/hostpattern`**: lenient `Match` for the redirect policy (an unparseable entry must match nothing, not everything) and strict `Validate` for the config edge (a port is refused rather than silently ignored), with fuzz targets pinning that a match never widens a grant and that a validated pattern is never silently dead. **The acceptance criterion is a boot test**: `test/offline` runs the real `serve` with `http.DefaultTransport` swapped for a refusing recorder, asserts zero outbound requests and zero repository rows, and carries a self-test proving the swap works. No API/CLI surface lists presets by name — the plan scopes this to data plus docs, and C-016's create endpoint takes the config directly. CI green (run 34127467142). Details → `docs/notes/phase-4.md` |
| C-015 | Proxy adversarial suite (living) | done | C-004…C-013 ✓ | `test/proxyadv`: 17 scenarios over the **composed** pipeline (real client + filler + stores) against an upstream that is actively wrong, with a **ratchet** — each case carries a stable id and its requirement in words, and `requiredScenarios` fails the suite if one loses coverage. **Named hosts via a routing transport**, because every httptest server is loopback and the policy refuses private addresses *before* the trusted-host list: without the mapping, "refused because untrusted" and "refused because private" are indistinguishable and a bug in either passes as the other. The SSRF case points a redirect at a **live loopback listener** and asserts nothing arrived, and the trusted-host half proves the harness follows redirects it is allowed to (C-014's `TrustedHosts`); the `WWW-Authenticate` realm gets the same treatment with real credentials configured. Traversal is asserted as a `*proxy.ReferenceError` rather than any error — an incidental failure would leave the next hostile name to chance — and the remainder half drives the two gates that exist (`Split`, `RoutingRules.Evaluate`), with the namespace rewrite's assertion written in as waiting on the `/v2/` wiring. Harness self-tests prove the machinery can observe a violation. **The push-latency gate was fixed alongside** (§9: a flake is fixed the day it flakes): it failed on this test-only commit because `ubuntu-latest` is not one machine — the same benchmark measured 79/96/103/134 ms across four runners with an identical allocation profile — so CI now measures the **parent commit on the same runner** and enforces against that, with the committed baseline demoted to advisory drift context. C-015's own run (34151507639) failed on that gate and nothing else; the suite is proven green by run **34152174411**, which carries it plus the fix, and where the same-machine comparison reads +0.1% on the benchmark the old gate called +38%. Details → `docs/notes/phase-4.md` |
| C-016 | Repository admin CRUD API (hosted/proxy/group) | done | C-001 ✓, D-020 ✓ | `internal/server/api_repositories.go` — five ADR 0015 routes over entities: create (`repo:create`@system, through `repo.ValidateEntityName` so `system` stays reserved), filtered list + get (`repo:list`), config update (`repo:configure`, **∧ `proxy:write` for a proxy**), delete (`repo:delete`, hosted requiring `?confirm=<name>` because the store now cascades content). A proxy's config is readable only with `proxy:read` and **omitted rather than nulled** without it. Config history (migration 0005, `UpdateRepositoryConfig` gains actor+time) writes the superseded revision in the same transaction and dies with its entity, so a recreated name inherits no lineage. **Five verbs off the §9 pending list (18 left)**: repo:create/configure/delete, proxy:read/write, each proven both ways against ADR 0002's splits. CI green (run 33900392222). Details → `docs/notes/phase-4.md` |

---

## Phase 5 — Scanning and vulnerability assessment

Detailed specs (files, test plans): `docs/plan/phase-5-scanning.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| S-001 | `scan.Scanner` interface + fake | done | D-002 ✓ | `internal/scan` + `internal/scan/fake`. **Interface split** beyond the ADR's sketch: `Scanner` (identity, `DatabaseVersion`, `Scan`) and `DatabaseUpdater`, recomposed as `Engine` — the queue and the gate depend only on `Scanner`, which never needs `UpdateDB`. Call contract frozen in the doc: nil error ⇒ `StatusSucceeded` is the *only* readable-as-clean outcome, and a failure returns a `StatusFailed` report too so a caller records the attempt rather than reconstructing it. Normalised model only — `Rollup` is field-for-field identical to `event.SeverityCounts` with a **reflection test that fails if either drifts**; `Clean()` is the only way to ask, because an empty findings list is also true of a failure. The fake **never invents an outcome**: an unscripted digest is `ErrNotScripted` (deliberately not wrapping `ErrScanFailed` — a missing test line and a broken scanner are different problems), and it validates its own scripted output. Vendor quarantine broadened to any scanner and **proven to bite** on synthetic graphs. Two ADR ambiguities resolved into an ADR 0017 clarifications section. Details → `docs/notes/phase-5.md` |
| S-002 | Scanner adapter + result normalisation | todo | D-002 ✓ | Vendor JSON never persisted as system of record |
| S-003 | Async scan queue + retry/backoff | todo | S-001 | Push latency unaffected under scan backlog |
| S-004 | CVE DB lifecycle: online update + offline import | todo | S-002 | `trove db import` tested air-gapped |
| S-005 | Rescan on DB update + `scan.regressed` event | todo | S-004, E-001 | Previously clean image re-flags on new CVE data |
| S-006 | Vulnerability model, rollups, queries | todo | S-002, Z-012 ✓ | Fixable/not-fixable split; results permission-filtered |
| S-007 | VEX / suppression rules | todo | S-006 | Auditable; suppressions expire |
| S-008 | Attach scan results as OCI referrers | todo | R-005 ✓, S-002 | Readable by external tooling; survives migration |
| S-009 | Scan-on-cache-fill for proxied content | todo | C-004, S-003 | Cached images not silently exempt from gating |
| S-010 | Registry-wide scan config with filters | todo | S-003 | Include/exclude by repo pattern |
| S-011 | Pull gating / quarantine enforcement | todo | D-015 ✓, Q12 ✓, Q24 ✓ | Off by default; serve-on-fill with per-policy strict mode; no bypass via digest, referrers, or group member; `gate:override` audited |

---

## Phase 6 — Policies, retention, GC, quotas

Detailed specs (files, test plans): `docs/plan/phase-6-policy-gc-quota.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| P-001 | Tag policy: immutability + prefix exceptions, protected tags | todo | R-003 ✓ | Protected beats every retention rule — adversarially tested |
| P-002 | Retention evaluator as a pure function | done | D-008 ✓, D-022 ✓ | `internal/policy`: `Evaluate(inventory, rules, now) → Plan`, zero I/O, own value types. **The selectable-set invariant is the design**: protected, immutable, index-child and live-subject manifests are excluded at inventory construction, so no rule, priority or count can reach them — asserted both by construction and by evaluation. **A fuzzer found a real hole**: an `untagged` rule legitimately selected an image whose *protected* attestation the ADR 0011 cascade then deleted — protection defeated through a path no rule traversed. Fixed by re-checking exclusions across cascade members, generalised into an ADR 0011 clarification. Equal-priority disagreement is a typed error naming every rule and yields **no plan at all**; lower priority wins (ECR prior art) and the deciding rule is first by *name*, so reordering a policy document cannot change a plan. Five fuzz targets incl. order-independence and protection dominance. Archtest rule added (state-reaching only — stdlib I/O is impossible to express transitively and is carried by a file-scoped purity test). Details → `docs/notes/phase-6.md` |
| P-003 | `keep-if-pulled-since` rule | todo | P-002, R-010 ✓ | Uses pull statistics; clock-injected tests |
| P-004 | Dry-run plan output (API + CLI) | todo | P-002 | Plan explains *why* each manifest is selected; gated on `policy:read` |
| P-005 | Apply path with audit records | todo | P-004, Z-005 ✓ | Requires `policy:apply` and explicit confirmation |
| P-006 | Scheduled policy runs / task scheduler | todo | P-005 | Overlapping runs cannot double-delete |
| P-007 | Mark-and-sweep GC, resumable | done | D-008 ✓, F-009 ✓ | `internal/gc` over the store layer of commit 1. **Rows before bytes**: the row goes in a transaction that re-checks every condition, the bytes follow, and a failure between them is a counted `Leaked` rather than an error — the reverse order would create a row whose content is missing, the one corruption this design refuses. **Interrupted ≠ failed**: a cancelled sweep saves its cursor and leaves the run open to resume, a store failure closes it with the reason; the tests caught the first implementation closing on cancellation, which made "resumable" a comment. A resumed sweep **keeps its original deadline** (proven by uploading a blob mid-run, advancing the clock past the grace window, and asserting it survives). A re-check that refuses — or that cannot answer — leaves the blob wholly intact and counts `Skipped`. **`blob.HostedRef` found its consumer**: `gc.reclaim` is the only function that deletes hosted bytes, and C-013's newtype pair is now load-bearing on both sides. **ADR 0009's proving test gained its converse** — `test/separation` now sweeps the hosted family and asserts every cached row and byte survives, over one fixture holding the same digests in both. `event.GCCompletedPayload.manifests_scanned` became `blobs_scanned`: nothing walks manifests any more, and no consumer exists yet. `internal/gc` at 100%. CI green (run 34153682877). **P-008's race matrix is next.** Details → `docs/notes/phase-6.md` |
| P-008 | GC race tests | review | P-007 ✓ | Two halves. **`internal/gc/race_test.go`** places a push at an exact point in a sweep — the sync points are the collector's own store calls, so no test-only hook was added to production code: GC loses to a manifest pushed between the listing and the delete, loses to an upload session opened there, survives a manifest deleted mid-sweep (eventually complete, not exactly complete), keeps a blob created *at* the grace deadline, and is interrupted at all four phase boundaries with the same two assertions each. **`test/gcrace`** lets nobody choose the interleaving: eight clients push complete images in the registry's own order against a collector sweeping the real SQLite + fs stores, asserting the invariant *every blob a live manifest references still has its row and its bytes*, with guards that garbage was actually collected and that deletes happened **while pushes ran**. **The concurrent suite found a production bug**: SQLite reports a cancelled context as `interrupted (9)`, so a shutdown read as a failed sweep — the collector now classifies by what was asked, not what the driver called it, or P-006's scheduler would log a failure on every shutdown. Chaos runs in the normal suite bounded to ~2s rather than nightly (no nightly workflow exists; a bounded test on every commit is worth more). `internal/gc` still 100%. Details → `docs/notes/phase-6.md` |
| P-009 | Quota accounting + enforcement | todo | D-016 ✓, Q8 ✓ | Per-repo + global; soft-warn event then hard-deny push; cache breach evicts harder, never fails pulls |
| P-012 | `trove verify` integrity scrub | todo | F-006 ✓, F-008 ✓ | Re-verifies blob digests; detects meta↔blob drift; read-only; referenced by the restore procedure (DOC-002) |

---

## Phase 7 — Events, observability, operability

Detailed specs (files, test plans): `docs/plan/phase-7-events-observability.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| E-001 | In-process event bus + typed event taxonomy | done | D-014 ✓, F-006 ✓ | `internal/event`: 16 ADR 0012 types (§8's 13 plus `group.member.skipped`, `cache.stale-served`, `blob.corrupt` — the ADR names them), each with a typed payload that makes a body disagreeing with its type unconstructable and a **round-trip** golden, so a field that encodes but never decodes fails. Bus registers handlers rather than handing out channels — a panicking channel *reader* panics where the bus cannot see it, so the required isolation test would have had nothing to assert; publish never blocks, full queues drop and count. **Outbox adds `meta.Store.WithinTx`** (an `AppendEvents` batch was rejected: no caller owns a transaction today, so it could only have delivered atomicity within itself, not ADR 0012's event-iff-committed). Migration 0006; no FK on `repo_name`, since an event outlives what it names. Wired into serve, drained before the store closes. Emission from call sites belongs to each owning task. CI green (d928a0a). Details → `docs/notes/phase-7.md` |
| E-002 | Webhook subscriptions + filters | todo | E-001 | Per-repo, per-event-type |
| E-003 | Webhook delivery: HMAC signing, retry, DLQ | todo | E-002 | At-least-once; documented idempotency key; dead-letter visible; delivery queue durable across restart |
| E-004 | **Webhook permission scoping** | todo | E-002, Z-012 ✓ | Subscription only receives events its owning subject can read |
| E-005 | Prometheus metrics | todo | E-001 | Request, storage, cache-hit, upstream headroom, scan queue, authz denials, GC progress; `/metrics` endpoint auth decided and enforced |
| E-006 | Metric label cardinality + disclosure review | todo | E-005 | Repo names as labels justified or removed |
| E-007 | `/healthz` and `/readyz` | todo | F-004 ✓ | Distinct semantics; readiness covers migrations and deps |
| E-008 | Read-only / maintenance mode | todo | F-003 ✓, Z-005 ✓ | Runtime toggle behind `system:maintenance`; writes rejected clearly |
| E-009 | Audit log (append-only, queryable) | todo | F-006 ✓ | Every mutating action with actor, digest, rule; gated on `audit:read` |
| E-010 | Cross-repo search (permission-filtered) | todo | Z-012 ✓, S-006 | By name, tag, digest, media type, CVE ID |
| E-011 | `trove support-bundle` | todo | F-003 ✓ | Config redacted, version, migration state, role/binding summary, recent logs |
| E-012 | Webhook target validation (SSRF) | todo | E-002 | Private/link-local ranges blocked by default; operator allowlist override; redirects re-validated |
| E-013 | Audit log retention + export rotation | todo | E-009 | Bounded growth; export before prune; the prune itself is audited |

---

## Phase 8 — UI, packaging, docs

Detailed specs (files, test plans): `docs/plan/phase-8-ui-packaging-docs.md`

| ID | Task | Status | Depends on | Acceptance criteria |
|---|---|---|---|---|
| U-001 | UI scaffold on chosen stack | todo | D-005 ✓ | Svelte 5 + Vite per ADR 0019; builds offline; embedded via `go:embed` |
| U-002 | Effective-permissions endpoint + UI gating | todo | U-001, Z-013 ✓ | Unusable actions hidden; server-side authz still enforced independently |
| U-003 | Repo/tag/manifest browsing | todo | U-002, R-003 ✓ | Copyable digests; dense tables; keyboard navigable |
| U-004 | SBOM + CVE report views | todo | U-002, S-006 | Filter by severity and fixability |
| U-005 | Policy editor with dry-run preview | todo | U-002, P-004 | Shows the plan before apply |
| U-006 | Proxy/group management screens | todo | U-002, C-011 | Upstream health, cache hit ratio, rate-limit headroom, member order |
| U-007 | **Role, binding, and user admin screens** | todo | U-002, Z-013 ✓ | Create roles, bind to scopes, explain effective permissions inline |
| U-008 | Webhook management + delivery history | todo | U-002, E-003 | Dead-letter inspection and replay |
| U-009 | Audit log viewer | todo | U-002, E-009 | Filter by actor, verb, resource, outcome |
| U-010 | Accessibility + dark mode pass | todo | U-003..U-009 | WCAG AA contrast; real focus states |
| PK-001 | systemd unit + `.deb`/`.rpm` | todo | F-001 ✓ | Zero-edit happy path |
| PK-002 | `docker-compose.yml` | todo | F-001 ✓ | Zero-edit happy path |
| PK-003 | Helm chart | todo | F-001 ✓ | Zero-edit happy path |
| PK-004 | TLS: ACME and operator-supplied certs | todo | F-003 ✓ | One config key switches modes |
| PK-005 | Cross-compiled release pipeline | todo | F-001 ✓ | linux/amd64, linux/arm64, darwin/arm64; checksums |
| PK-006 | `trove migrate --from` import | todo | Q17 ✓ | Generic distribution-spec source (catalog walk or repo list); resumable |
| DOC-001 | Quickstart: TLS registry + Docker Hub cache + admin and developer roles in <5 min | todo | PK-001, PK-004, C-005, Z-014 ✓ | Verified end to end on a clean VM |
| DOC-002 | Operator guide: backup, restore, upgrade, GC, read-only mode | todo | P-007, E-008 | Restore path actually tested |
| DOC-003 | **RBAC guide: roles, scopes, common setups, troubleshooting** | done | Z-013 ✓ | `docs/operator/rbac.md`: the model digested for operators — three concepts, the four scope forms with the two that surprise people (`system` and `*` are disjoint; `team-a/*` excludes `team-a`), the verb splits that exist because collapsing them is an incident, and all six built-ins with their exact verb sets. Four worked setups (three-role team, per-team with groups, CI robots scoped to blast radius, anonymous enablement) expressed as the **bindings** to create, plus a troubleshooting section leading with the confusion that actually happens: not-found means no-permission, and `trove auth explain` is how you tell them apart — including the disabled-subject case where grants are on the books and nothing works. Carries the ADR 0001 naming-discipline note (no carve-outs, so exceptions live in the namespace) and the ADR 0003 `auditor` warning (`audit:read` is global visibility). **States plainly that subject/role/binding administration lands with U-007** rather than documenting commands that do not exist. Every fact checked against the code. |

---

## Decisions — all questions answered (2026-09-01)

Full statements live in CLAUDE.md §13; ADR tasks D-002…D-022 formalise them.

| ID | Decision |
|---|---|
| Q1 | `trove`, module `github.com/steveokay/trove` |
| Q2 | Trivy as a Go library, behind `scan.Scanner` in the adapter package |
| Q3 | Local users, robot accounts, local groups only in v1; OIDC → local groups in v1.1 |
| Q4 | Store + display signatures; gating may check presence; full verification v1.1 |
| Q5 | Single node for v1; in-process locking and single-flight |
| Q6 | Air-gapped install + offline CVE DB import are v1 requirements |
| Q7 | Presets for Docker Hub, ghcr, quay, registry.k8s.io, gcr — all disabled by default |
| Q8 | Per-repo + global quotas; soft-warn then hard-deny push; cache evicts, never fails pulls |
| Q9 | Apache-2.0 |
| Q10 | Deleting an index-referenced child manifest is an error naming the index |
| Q11 | Global cache budget (50 GB default) + LRU, per-proxy override; tag TTL 15m; negative TTL 60s |
| Q12 | Pull gating off by default — observe first |
| Q13 | Blob encryption delegated to filesystem/S3 layer; app encrypts secrets only |
| Q14 | RBAC additive-only, no deny rules — confirmed |
| Q15 | Promotion deferred to v1.1 (P-011 dropped) |
| Q16 | Immediate delete; GC lag is the grace window (P-010 dropped) |
| Q17 | Generic importer for any distribution-spec-compliant source, resumable |
| Q18 | Unauthorized reads return 404 / `NAME_UNKNOWN`, uniformly; unauthenticated gets 401 + challenge |
| Q19 | Bindings attach to subjects and local groups/teams |
| Q20 | Scopes are repository patterns only; tag needs met by per-repo tag policies |
| Q21 | Auto-generated 32-byte keyfile (0600), AES-256-GCM, re-encrypt rotation command |
| Q22 | Subject deletion cascade-deletes its referrers, each audited |
| Q23 | CLI is an admin-API client (`trove login`/`TROVE_TOKEN`); `serve`, `version`, `--offline db import` excepted |
| Q24 | Cache-fill pull served immediately; async scan gates subsequent pulls; per-policy strict mode |
| Q25 | Native Windows inner loop (revised 2026-09-01); `make test-linux` in a Debian-based `golang` container with docker-socket mount; Linux CI authoritative |

---

## Dropped

| ID | Task | Reason |
|---|---|---|
| Z-017 | External identity (OIDC/LDAP) → binding mapping | Q3 decided: local subjects + groups only in v1; OIDC maps onto local groups in v1.1 |
| P-010 | Soft delete / recovery window | Q16 decided: immediate delete, GC lag is the grace window; revisit in v1.1 if demanded |
| P-011 | Promotion between repositories | Q15 decided: deferred to v1.1 — wants only-if-scanned/signed policy hooks that mature with gating |
