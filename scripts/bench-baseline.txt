# Push-latency baseline (R-012): benchmark name and ns/op.
#
# ADVISORY, not a gate. These numbers came from one CI runner, and the hosted
# pool spans hardware that measures the 100 MiB push anywhere from 79ms to
# 134ms with identical code. The gate compares the commit under test against
# its parent on one machine (see the bench job in .github/workflows/ci.yml);
# this file is read alongside it as long-run drift context and never fails a
# build. Refresh it from the bench-baseline-candidate artifact when the numbers
# here stop resembling reality.
#
# Recorded with: -benchtime=1s -count=3, best of 3.
BenchmarkMonolithicBlobPush1MiB 1321563
BenchmarkMonolithicBlobPush100MiB 97251049
BenchmarkChunkedBlobPush 1364841
BenchmarkManifestPut 16260
